import UIKit

struct ImageResources {
    static let profile = UIImage(systemName: "person.crop.circle")
    static let memo = UIImage(systemName: "list.clipboard.fill")
    static let debug = UIImage(systemName: "gamecontroller")
    static let edit = UIImage(systemName: "person.crop.square")
}
